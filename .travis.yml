language: cpp

os:
  - linux
  - osx

dist: bionic
osx_image: xcode10.3

compiler:
  - gcc
  - clang

cache:
  directories:
    - ./benchmarks
    - ./tools/sdf3*/
     
addons:
  homebrew:
    packages:
    - glpk
    update: true
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - libglpk-dev
    - glpk-utils
    - libboost-all-dev

script:
#  - make sdf3_build
#  - cd tools/sdf3/sdf3 && make
  - make unit_test
  - make test